<%= form_with(model: claim, local: true) do |form| %>
  <% if claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(claim.errors.count, "error") %> prohibited this claim from being saved:</h2>

      <ul>
      <% claim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :claim_date %>
    <%= form.date_select :claim_date, id: :claim_claim_date %>
  </div>

  <div class="field">
    <%= form.label :pharmacy %>
    <%= form.text_field :pharmacy, id: :claim_pharmacy %>
  </div>

  <div class="field">
    <%= form.label :drug %>
    <%= form.text_field :drug, id: :claim_drug %>
  </div>

  <div class="field">
    <%= form.label :prescription_cost %>
    <%= form.text_field :prescription_cost, id: :claim_prescription_cost %>
  </div>

  <div class="field">
    <%= form.label :plan_paid %>
    <%= form.text_field :plan_paid, id: :claim_plan_paid %>
  </div>

  <div class="field">
    <%= form.label :patient_paid %>
    <%= form.text_field :patient_paid, id: :claim_patient_paid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
